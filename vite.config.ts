import { defineConfig } from 'vite'

// Base Vite configuration for the project
// Extended by vitest.workspace.ts for Storybook testing
export default defineConfig({
  // Add any project-specific Vite configuration here
})
