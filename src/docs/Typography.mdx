import { Meta, Typeset } from '@storybook/addon-docs/blocks'

<Meta title="Design System/Typography" />

# Typography System

MUI's typography system provides consistent text styles following Material Design guidelines.

---

## Default Font Stack

```tsx
fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif'
```

---

## Type Scale

<table>
  <thead>
    <tr>
      <th>Variant</th>
      <th>Size</th>
      <th>Weight</th>
      <th>Line Height</th>
      <th>Use Case</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><code>h1</code></td><td>96px (6rem)</td><td>300</td><td>1.167</td><td>Hero headers</td></tr>
    <tr><td><code>h2</code></td><td>60px (3.75rem)</td><td>300</td><td>1.2</td><td>Page titles</td></tr>
    <tr><td><code>h3</code></td><td>48px (3rem)</td><td>400</td><td>1.167</td><td>Section headers</td></tr>
    <tr><td><code>h4</code></td><td>34px (2.125rem)</td><td>400</td><td>1.235</td><td>Subsections</td></tr>
    <tr><td><code>h5</code></td><td>24px (1.5rem)</td><td>400</td><td>1.334</td><td>Card titles</td></tr>
    <tr><td><code>h6</code></td><td>20px (1.25rem)</td><td>500</td><td>1.6</td><td>Small headers</td></tr>
    <tr><td><code>subtitle1</code></td><td>16px (1rem)</td><td>400</td><td>1.75</td><td>Emphasized body</td></tr>
    <tr><td><code>subtitle2</code></td><td>14px (0.875rem)</td><td>500</td><td>1.57</td><td>Labels</td></tr>
    <tr><td><code>body1</code></td><td>16px (1rem)</td><td>400</td><td>1.5</td><td>Primary content</td></tr>
    <tr><td><code>body2</code></td><td>14px (0.875rem)</td><td>400</td><td>1.43</td><td>Secondary content</td></tr>
    <tr><td><code>button</code></td><td>14px (0.875rem)</td><td>500</td><td>1.75</td><td>Buttons</td></tr>
    <tr><td><code>caption</code></td><td>12px (0.75rem)</td><td>400</td><td>1.66</td><td>Help text</td></tr>
    <tr><td><code>overline</code></td><td>12px (0.75rem)</td><td>400</td><td>2.66</td><td>Labels</td></tr>
  </tbody>
</table>

---

## Typography Component

### Basic Usage

```tsx
import { Typography } from '@mui/material'

<Typography variant="h1">Heading 1</Typography>
<Typography variant="body1">Body text</Typography>
<Typography variant="caption">Caption text</Typography>
```

### Semantic HTML

```tsx
// Renders as <p> with h4 styles
<Typography variant="h4" component="p">
  Styled as h4, semantically a paragraph
</Typography>

// Renders as <h1> with h3 styles
<Typography variant="h3" component="h1">
  Styled as h3, semantically an h1
</Typography>
```

### Props

```tsx
<Typography
  variant="body1"      // Style variant
  component="span"     // HTML element
  color="primary"      // Text color
  align="center"       // Text alignment
  noWrap              // Truncate with ellipsis
  gutterBottom        // Add bottom margin
  paragraph           // Add bottom margin (body only)
>
  Content
</Typography>
```

---

## Font Weights

```tsx
theme.typography = {
  fontWeightLight: 300,
  fontWeightRegular: 400,
  fontWeightMedium: 500,
  fontWeightBold: 700,
}
```

---

## Customizing Typography

### Custom Font Family

```tsx
const theme = createTheme({
  typography: {
    fontFamily: [
      'Inter',
      '-apple-system',
      'BlinkMacSystemFont',
      '"Segoe UI"',
      'Roboto',
      'Arial',
      'sans-serif',
    ].join(','),
  },
})
```

### Custom Variant Styles

```tsx
const theme = createTheme({
  typography: {
    h1: {
      fontSize: '3rem',
      fontWeight: 700,
      lineHeight: 1.2,
      letterSpacing: '-0.01em',
    },
    body1: {
      fontSize: '1rem',
      lineHeight: 1.6,
    },
    button: {
      textTransform: 'none', // Remove uppercase
      fontWeight: 600,
    },
  },
})
```

### Responsive Typography

```tsx
const theme = createTheme({
  typography: {
    h1: {
      fontSize: '2rem',
      [theme.breakpoints.up('sm')]: {
        fontSize: '3rem',
      },
      [theme.breakpoints.up('md')]: {
        fontSize: '4rem',
      },
    },
  },
})
```

### Add Custom Variants

```tsx
// 1. Extend theme type
declare module '@mui/material/styles' {
  interface TypographyVariants {
    poster: React.CSSProperties
  }
  interface TypographyVariantsOptions {
    poster?: React.CSSProperties
  }
}

declare module '@mui/material/Typography' {
  interface TypographyPropsVariantOverrides {
    poster: true
  }
}

// 2. Create theme
const theme = createTheme({
  typography: {
    poster: {
      fontSize: '4rem',
      fontWeight: 700,
      lineHeight: 1.1,
    },
  },
})

// 3. Use it
<Typography variant="poster">Large Poster</Typography>
```

---

## Using Typography Tokens

### In sx Prop

```tsx
<Box
  sx={{
    typography: 'body1',
    // or individual properties
    fontWeight: 'fontWeightBold',
    fontSize: 'h6.fontSize',
    lineHeight: 'body1.lineHeight',
  }}
/>
```

### In styled Components

```tsx
const CustomText = styled('span')(({ theme }) => ({
  ...theme.typography.body2,
  fontWeight: theme.typography.fontWeightMedium,
}))
```

---

## Code Typography

```tsx
const theme = createTheme({
  typography: {
    fontFamily: '"Roboto", sans-serif',
    // Monospace for code
    fontFamilyCode: '"Roboto Mono", "Consolas", monospace',
  },
})

// Usage
<Box sx={{ fontFamily: 'fontFamilyCode' }}>
  const x = 42;
</Box>
```

---

## Text Utilities

### Truncation

```tsx
// Single line truncation
<Typography noWrap>
  Long text that will be truncated with ellipsis...
</Typography>

// Multi-line truncation
<Typography
  sx={{
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    display: '-webkit-box',
    WebkitLineClamp: 3,
    WebkitBoxOrient: 'vertical',
  }}
>
  Multi-line text that truncates after 3 lines...
</Typography>
```

### Colors

```tsx
<Typography color="primary">Primary color</Typography>
<Typography color="secondary">Secondary color</Typography>
<Typography color="textPrimary">High emphasis</Typography>
<Typography color="textSecondary">Medium emphasis</Typography>
<Typography color="error">Error state</Typography>
```

---

## Best Practices

<table>
  <thead>
    <tr>
      <th>Practice</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><strong>Use variants</strong></td><td>Prefer <code>variant</code> over custom styles</td></tr>
    <tr><td><strong>Semantic HTML</strong></td><td>Use <code>component</code> for correct semantics</td></tr>
    <tr><td><strong>Limit variants</strong></td><td>Stick to 4-5 variants per page</td></tr>
    <tr><td><strong>Consistent sizing</strong></td><td>Follow the type scale</td></tr>
    <tr><td><strong>Responsive</strong></td><td>Adjust sizes for mobile</td></tr>
  </tbody>
</table>
