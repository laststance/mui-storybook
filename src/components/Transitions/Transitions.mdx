import { Meta, Primary, Controls, Canvas, Stories } from '@storybook/addon-docs/blocks'
import { En, Ja } from '../../../.storybook/components/LocalizedDoc'
import * as TransitionsStories from './Transitions.stories'

<Meta of={TransitionsStories} />

<En>
# Transitions

Transition animations

## Overview

MUI Transitions are a set of components for smoothly animating the display and hiding of elements. They provide various animation effects including Collapse (height animation), Fade (opacity animation), Grow (scale and fade), Slide (slide), and Zoom (zoom). By applying them to dynamic UI elements such as dialogs, menus, and notifications, you can achieve a polished user experience.
</En>

<Ja>
# Transitions

トランジションアニメーション

## 概要

MUI Transitions は、要素の表示・非表示をスムーズにアニメーション化するコンポーネント群です。Collapse (高さのアニメーション)、Fade (透明度のアニメーション)、Grow (拡大と透明度)、Slide (スライド)、Zoom (ズーム) など、様々なアニメーション効果を提供します。ダイアログ、メニュー、通知などの動的な UI 要素に適用することで、洗練されたユーザーエクスペリエンスを実現できます。
</Ja>

## Primary Demo

<Primary />

<En>
## Interactive Controls

Use the controls below to modify properties:
</En>

<Ja>
## インタラクティブコントロール

以下のコントロールを使用してプロパティを変更できます:
</Ja>

<Controls />

<En>
## All Variants
</En>

<Ja>
## すべてのバリエーション
</Ja>

<Stories />

<En>
## Usage Example

```tsx
import Fade from '@mui/material/Fade'
import Grow from '@mui/material/Grow'
import Slide from '@mui/material/Slide'
import Box from '@mui/material/Box'
import { useState } from 'react'

function TransitionExample() {
  const [show, setShow] = useState(false)

  return (
    <>
      <button onClick={() => setShow(!show)}>Toggle</button>

      <Fade in={show}>
        <Box sx={{ mt: 2 }}>Fading content</Box>
      </Fade>

      <Grow in={show}>
        <Box sx={{ mt: 2 }}>Growing content</Box>
      </Grow>

      <Slide direction="up" in={show}>
        <Box sx={{ mt: 2 }}>Sliding content</Box>
      </Slide>
    </>
  )
}
```

## Accessibility

- Animations are visual aids and not essential to functionality
- Respects the `prefers-reduced-motion` media query
- Content remains accessible during animations
- Screen readers announce content normally
- Keyboard focus is managed appropriately
</En>

<Ja>
## 使用例

```tsx
import Fade from '@mui/material/Fade'
import Grow from '@mui/material/Grow'
import Slide from '@mui/material/Slide'
import Box from '@mui/material/Box'
import { useState } from 'react'

function TransitionExample() {
  const [show, setShow] = useState(false)

  return (
    <>
      <button onClick={() => setShow(!show)}>トグル</button>

      <Fade in={show}>
        <Box sx={{ mt: 2 }}>フェードするコンテンツ</Box>
      </Fade>

      <Grow in={show}>
        <Box sx={{ mt: 2 }}>拡大するコンテンツ</Box>
      </Grow>

      <Slide direction="up" in={show}>
        <Box sx={{ mt: 2 }}>スライドするコンテンツ</Box>
      </Slide>
    </>
  )
}
```

## アクセシビリティ

- アニメーションは視覚的な補助であり、機能に必須ではありません
- `prefers-reduced-motion` メディアクエリを尊重します
- アニメーション中もコンテンツはアクセス可能な状態を維持します
- スクリーンリーダーはコンテンツを通常通り読み上げます
- キーボードフォーカスは適切に管理されます
</Ja>
